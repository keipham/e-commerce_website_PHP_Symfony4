{% extends 'base.html.twig' %}

{% block title %}Nos Formules{% endblock %}

{% block body %}
    <h1 class="mt-2 ml-3">Nos Formules</h1>
        
        <div class="w-body">

            <h2>Plus de jeux, plus de fun !</h2>
            Avec nos formules, ne réfléchissez plus.<br>
            Vous avez le choix de prendre de 1 à 4 jeux sur une ou plusieurs journées.<br> <br> 
        </div>

        <div class="w-body">
   
        <div class="row mt-4 bande">
        {% for formula in formulas %}
            <div class="col s12 m4">
              <img class="img-border" src="/images/{{ formula.picture }}" alt="logo-escape-game" width="100%">
              <a href="{{ path('formulas_show', {'id': formula.id}) }}" class=" btn btn-secondary middle">En savoir plus</a>
            </div>
        {% endfor %}
        </div>


       

    </tbody>
    <a class="btn btn-secondary" href="/">Retour à l'accueil</a>


{% endblock %}

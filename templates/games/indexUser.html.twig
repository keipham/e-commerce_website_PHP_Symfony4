{% extends 'base.html.twig' %}

{% block title %}Nos Jeux{% endblock %}

{% block body %}
    
        <div class="w-body">
            <h1 class="mt-2 ml-3">Nos Jeux</h1>
                 <h2> 4 scénarios au choix! </h2>
                    <p> Nous avons une selection de plusieurs jeux qui vous feront tous frissoner de peur. Notre sélection est variée et conviendra à tous. </p>
                
                <div class="row mt-4 bande">  
                    {% for game in games %}
                        <div class="col s12 m4">
                            <img class="img-border" src="/images/{{ game.picture }}" alt="logo-escape-game" width="100%">
                            <a href="{{ path('games_show_users', {'id': game.id}) }}" class=" btn btn-secondary middle">En savoir plus</a>
                        </div>
                    {% endfor %}
                </div>
                    
        </div>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="/">Retour à l'accueil</a>
{% endblock %}

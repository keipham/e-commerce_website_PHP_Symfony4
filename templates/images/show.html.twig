{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr> #}
            <tr>
                <th>Nom de l'image</th>
                <td>{{ image.image }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{{ image.updatedAt ? image.updatedAt|date('Y-m-d ') : '' }}</td>
            </tr>
             <tr>
                <th>Image</th>
                <td><img src="/images/pictures_clients/{{ image.image }}" width="1500px"/></td>
            </tr>
        </tbody>
    </table>

    {# <a class="btn btn-secondary "href="{{ path('admin_pictures_index') }}">Retour</a> #}

    {{ include('images/_delete_form.html.twig') }}
{% endblock %}

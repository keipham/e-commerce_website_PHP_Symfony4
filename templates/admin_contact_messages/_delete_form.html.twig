<form class="btn btn-danger" method="post" action="{{ path('admin_contact_delete', {'id': contact_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_message.id) }}">
    <button class="btn"><strong>Delete</strong></button>
</form>
